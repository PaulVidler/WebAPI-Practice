// This file was created automatically by creating a webAPI project. Depending on the properties of the project being either
//  Development or Production in the environment variables, the values of the properties in this file will be different.
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // below, we are adding the mail settings to the appsettings.json file instead of hard coding those values in the mailService files.
  "mailsettings": {
    "mailToAddress": "developer@company.com",
    "mailFromAddress": "noreply@company.com"
  },
  "AllowedHosts": "*"
}

